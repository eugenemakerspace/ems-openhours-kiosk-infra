[Unit]
Description=EMS Open Hours static web server
After=network.target

[Service]
Type=simple
WorkingDirectory={{ web_content_dir }}
ExecStart=/usr/bin/python3 -m http.server {{ http_port }} --bind 127.0.0.1
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
